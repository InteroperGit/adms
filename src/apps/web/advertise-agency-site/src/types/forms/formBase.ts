/**
 * Базовый интерфейс для содержимого формы.
 *
 * Содержит только уникальный идентификатор записи и используется как основа
 * для других интерфейсов форм с дополнительными полями.
 *
 * @property {string} id - Уникальный идентификатор записи.
 *
 * @example
 * const base: BaseFormContent = {
 *   id: "abc123"
 * };
 */
export interface BaseFormContent {
    id: string;
}
