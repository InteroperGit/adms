/**
 * @description Navigation link with display label and destination URL.
 * Used in header navigation arrays and footer link lists.
 */
export interface NavLink {
  /** Display text for the link */
  label: string;
  /** Destination URL or path */
  href: string;
}
